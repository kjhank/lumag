import { TextWithMediaProps } from '@/components/TextWithMedia/TextWithMedia.types';
import { MiniCarousel } from '@/types';

export type PlatesLeaderSectionProps = {
  carryPlate: TextWithMediaProps;
  europeanLeader: MiniCarousel;
};
