import { ACFImage } from '@/types';

export type HeaderProps = {
  className?: string;
  background: ACFImage;
  footerText?: string;
  text?: string;
  title: string;
  verticalBackground?: ACFImage;
};
